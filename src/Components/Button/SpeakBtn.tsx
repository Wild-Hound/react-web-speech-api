import React from "react";
import Button from "react-bootstrap/Button";

const SpeakBtn = () => {
  return (
    <div>
      <Button className="btn-lg btn-block mb-3">Speak</Button>
    </div>
  );
};

export default SpeakBtn;
